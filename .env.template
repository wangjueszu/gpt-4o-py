# API Token (必填)
API_TOKEN=your_api_token_here

# 默认模型 (可选, 默认值: gpt-4o-image-vip)
MODEL=gpt-4o-image-vip

# 处理间隔时间 (秒, 可选，顺序处理版本使用)
# API_DELAY=2 

# 最大并发数量 (可选，并发处理版本使用，默认值: 5)
# MAX_WORKERS=5

# API限流间隔 (秒, 可选，并发处理版本使用，默认值: 0.5)
# API_RATE_LIMIT=0.5 